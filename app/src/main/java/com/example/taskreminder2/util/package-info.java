/**
 * Utility lintas-lapisan.
 *
 * <p>Berisi {@code OverdueChecker} — utility murni-hitung untuk status
 * terlambat (Fitur-08). Tidak pernah menulis ke DB: status overdue
 * DIHITUNG saat dibutuhkan dari (deadline, status, waktu sekarang),
 * tidak disimpan sebagai kolom (lihat blueprint Fitur-08).</p>
 */
package com.example.taskreminder2.util;
