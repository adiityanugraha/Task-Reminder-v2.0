/**
 * Lapisan database lokal Room (Personal Mode).
 *
 * <p>Memuat {@code AppDatabase} (kelas {@code RoomDatabase} abstrak yang
 * mendaftarkan semua Entity & DAO, version = 1, exportSchema = true).
 * Sub-package {@code entity} dan {@code dao} memuat tabel dan query-nya.</p>
 */
package com.example.taskreminder2.data.local;
