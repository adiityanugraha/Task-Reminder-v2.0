<?xml version="1.0" encoding="utf-8"?>
<!-- Kotak putih dengan border lembut (deadline picker, baris prioritas). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="14dp" />
    <solid android:color="@color/es_surface" />
    <stroke android:width="1.5dp" android:color="@color/es_border" />
</shape>
